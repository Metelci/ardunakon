package com.metelci.ardunakon.wifi

/**
 * Represents the connection state of the WiFi link.
 */
enum class WifiConnectionState {
    DISCONNECTED,
    CONNECTING,
    CONNECTED,
    ERROR
}
